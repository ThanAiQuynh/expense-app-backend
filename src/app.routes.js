import express from 'express';

const router = express.Router();

// Placeholder for auth routes
// import authRoutes from './modules/auth/client/route.js';
// router.use('/auth', authRoutes);

export default router;
